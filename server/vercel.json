{
  "version": 2,
  "builds": [
    {
      "src": "index.js",  
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/api/login", "dest": "/login" },
    { "src": "/api/signin", "dest": "/signin" },
    { "src": "/(.*)", "dest": "/" }  
  ]
}
